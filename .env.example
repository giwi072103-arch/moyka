DATABASE_URL=postgresql://wash:wash@localhost:5432/wash
PORT=3000
NODE_ENV=development
APP_URL=http://localhost:5173
# Token from BotFather. Never commit a real token.
BOT_TOKEN=
# Telegram numeric ID of the owner (not username).
ADMIN_TELEGRAM_IDS=
# Optional encrypted storage for workers' public transfer-card numbers.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CARD_ENCRYPTION_KEY=
TRUST_PROXY_HOPS=1
